project/
├── backend/
│   ├── main.py
│   ├── requirements.txt
│   └── ...
└── frontend/
    ├── package.json
        ├── vite.config.js
            └── ...
     pip install -r requirements.txt
            uvicorn main:app --host 0.0.0.0 --port 10000
            OPENAI_API_KEY → your keySOUNDFONT_PATH → /usr/share/sounds/sf2/FluidR3_GM.sf2
            npm install && npm run build
            dist
            VITE_API_URL
            https://pdf-song-backend.onrender.com
            export default defineConfig({
              plugins: [react()],
                server: {
                    proxy: {
                          "/compose": "https://pdf-song-backend.onrender.com",
                                "/download-audio": "https://pdf-song-backend.onrender.com"
                                    }
                                      }
                                      });
                                      